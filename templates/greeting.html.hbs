<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        /* Resetando margens e padding para uma aparência mais consistente */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Estilo para o body com fundo em degradê */
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #000000, #9b4d96); /* Degradê de preto para lilás */
            color: #fff;
            padding: 20px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* Estilo para o título principal */
        h1 {
            font-size: 2.5rem;
            color: #fff;
            margin-bottom: 20px;
            text-align: center;
        }

        /* Estilo para a mensagem */
        p {
            font-size: 1rem;
            color: #ddd; /* Texto mais suave */
            margin-bottom: 30px;
            text-align: center;
        }

        /* Estilo para os links */
        a {
            font-size: 1.1rem;
            color: #9b4d96; /* Cor lilás para links */
            text-decoration: none;
            margin: 10px 0;
            transition: color 0.3s ease, transform 0.3s ease;
        }

        a:hover {
            color: #7a3c7d; /* Cor mais escura para hover */
            transform: scale(1.05); /* Efeito de aumento ao passar o mouse */
        }

        /* Estilo responsivo */
        @media (max-width: 480px) {
            h1 {
                font-size: 2rem;
            }

            p {
                font-size: 0.9rem;
            }

            a {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <h1>{{greeting_message}}</h1>
    <p>Obrigado por enviar seus dados!</p>

    <!-- Link para a nova página de músicas favoritas -->
    <a href="/favorite-songs">Ver minhas músicas favoritas</a>
    <br/>
    <a href="/">Voltar à página inicial</a>
</body>
</html>
